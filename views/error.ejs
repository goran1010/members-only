<% if (locals.errors && errors.length > 0) { %>
<div class="alert">
  <% errors.forEach(error => { %>
  <p><%= error.msg %></p>
  <% }) %>
</div>
<% } %> <% if (locals.error && error.length > 0) { %>
<div class="alert">
  <% error.forEach(err => { %>
  <p><%= err %></p>
  <% }) %>
</div>
<% } %>
